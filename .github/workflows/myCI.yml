name: CI

# on:
#   push:
#     branches: [ main ]
#   pull_request:
#     branches: [ main ]

on:
  workflow_dispatch:

jobs:
  call-generic:
    uses: PremalWonderbiz/Workflows/.github/workflows/main-sequential-ci.yml@main
    secrets:
      SONAR_HOST_URL: ${{ secrets.SONAR_HOST_URL }}
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
    with:
      backend_sonar_key: "Device-Monitoring-System-Backends"
      frontend_sonar_key: "Device-Monitoring-System-Frontends"
