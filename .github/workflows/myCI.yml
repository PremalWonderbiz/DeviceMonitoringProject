name: Sonar Analysis

on:
  workflow_dispatch:

jobs:
  sonar:
    uses: PremalWonderbiz/Workflows/.github/workflows/main-sequential-ci.yml@main
    with:
      sonarAnalysisFile: "**/data.sql,**/AlarmRulesScripts/**"
    secrets: inherit
